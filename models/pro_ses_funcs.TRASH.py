

# 15 Aug 2018 

		# Clean Sessions 
		#rec_set = self.env[model].search([
		#										('procedure', '=', self.id), 
		#									])
		#ret = rec_set.unlink()


	# Clean 
	#rec_set = self.env['openhealth.session.med'].search([
	#														('procedure', '=', self.id), 
	#												])
	#ret = rec_set.unlink()

