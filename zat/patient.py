# -*- coding: utf-8 -*-
from openerp import fields, models



#class Partner(models.Model):
#    	_inherit = 'res.partner'

# Add a new column to the res.partner model, by default partners are not
# instructors
#		instructor = fields.Boolean("Instructor", default=False)

#		session_ids = fields.Many2many(
#				'openacademy.session',
#				string="Attended Sessions", 
#				readonly=True
#				)


#class OeHealthPatient(osv.osv):
#class OeHealthPatient(models.Model):
#class OeHealthPatient(osv.Model):


#class Patient(models.Model):

		#_name='oeh.medical.patient'
		#_name='patient_jx'

#		_inherit = 'oeh.medical.patient'

#		oh_instructor = fields.Boolean(
#				"jx Instructor", 
#				default=False,
#				)

#		oh_jx = fields.Char(
#				string="jx OH", 
#				default='Gotcha !!!', 
#				)
		
		

#		service_ids = fields.One2many(
#			'openhealth.service', 
#			'treatment_id', 
#			string="Services"
#			)


		#_column = {
		#		'my_field': fields.char(
		#			'My new field'
		#			)
		#		}

#		_column = {
				#'my_field': fields.char(
				#	'My new field'
				#	)
#				treatment_ids = fields.One2many(
#					'openhealth.treatment',
#					string="Treatments", 
#					)
#				}




		#treatment_ids = fields.Many2many(
		#x_treatment_ids = fields.One2many(
		#		'openhealth.treatment',
		#		string="Treatments", 
		#		)
				#readonly=True,


		#service_ids = fields.One2many(




